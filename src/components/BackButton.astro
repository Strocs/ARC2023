---
const { hoverBackgroundColor = 'hover:bg-white', link, Element } = Astro.props
---

<Element
	href={link}
	class={`${hoverBackgroundColor} group px-2 gap-1 py-1 w-fit transition-all duration-200 cursor-pointer flex  items-center self-end`}
>
	<slot />
</Element>
