---
const { textColor, mobile } = Astro.props
---

<div class={`gap-3 items-center ${mobile ? 'flex md:hidden' : 'hidden md:flex'}`}>
	<button
		class={`text-white outline outline-white outline-2 -outline-offset-[2px] py-1 px-3 font-normal bg-white ${textColor} hover:bg-transparent hover:text-white transition-all ease-in duration-200`}
		>Descarga la Programación</button
	>
</div>
