---
import LogoWithText from './LogoWithText.astro'
import Navbar from './Navbar.astro'
import SocialNetworks from './SocialNetworks.astro'
import ToggleMenuButton from './ToggleMenuButton.astro'
---

<header>
	<div class='flex w-full items-center justify-between sm:justify-start sm:gap-14 fixed top-0 bg-black bg-texture sm:bg-sm bg-xl sm:px-14 px-4 py-2 z-50'>
		<ToggleMenuButton id='menu' left />
		<div class='flex text-white justify-between w-full'>
			<LogoWithText />
			<div class="items-center gap-3 hidden lg:flex">
				<p class="w-full">Descarga el programa aquí: </p>
				<button class=" text-white border-2 rounded-md py-1 px-3 font-normal hover:bg-white hover:text-black transition-colors">Programa</button>
			</div>
			<SocialNetworks />
		</div>
		<ToggleMenuButton id='menu' />
	</div>
	<Navbar id='navbar' />
</header>

<script type='module'>
	const menu = document.querySelectorAll('#menu')
	const navbar = document.getElementById('navbar')

	function showNavBar(value) {
		if (value) {
			navbar.style.left = '0'
			navbar.style.opacity = '1'
		} else {
			navbar.style.left = '-320px'
		}
	}

	navbar.querySelectorAll('a').forEach(anchor => {
		anchor.addEventListener('click', () => {
			showNavBar(false)
		})
	})

	navbar.addEventListener('transitionend', () => {
		if (navbar.style.left === '-320px') {
			navbar.style.opacity = '0'
		}
	})

	menu.forEach(el => el.addEventListener('change', e => (e.target.checked ? showNavBar(true) : showNavBar(false))))
</script>
