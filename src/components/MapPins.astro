---
import { ARC_2023 } from '../data/ARC_2023'
import Pins from './Pins.astro'
const { locations } = ARC_2023
---

{
	locations.map((location) => (
		<Pins
			position={location.image.position}
			id={location.id}
			img={location.image.url}
			title={location.title}
		/>
	))
}
